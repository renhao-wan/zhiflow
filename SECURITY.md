# 安全策略

## 支持范围

当前维护 `0.1.x`。安全修复会优先进入最新版本；早期开发快照不单独维护。

## 私下报告安全问题

如果你发现密钥泄露、路径穿越、任意文件读写、服务端请求伪造或其他安全问题，请使用 GitHub 仓库的 **Security → Report a vulnerability** 私下报告。

请不要在公开 Issue、讨论区或截图中附上：

- API Key、Cookie、Token 或浏览器登录信息；
- `backend/.env`、Cookie 文件、SQLite 数据库或 Obsidian 仓库内容；
- 含用户名的绝对路径、私人媒体链接、完整转写稿或知识稿；
- 能直接访问你本机服务的地址或日志原文。

报告中可以提供最小复现步骤、受影响版本、预期与实际行为，以及已经脱敏的日志。维护者确认后会说明影响范围和修复安排。

如果密钥或 Cookie 已经公开，请先在对应平台撤销或轮换凭据，再提交报告。删除公开内容并不能保证旧凭据失效。

## 本地优先的边界

知流默认把数据库、下载文件、转写稿和导出内容保存在本机，并通过 `.gitignore` 与发布扫描阻止它们进入仓库。但以下行为仍会产生网络请求：

- 获取公开媒体页面、字幕、音视频流与封面；
- 用户启用 AI 总结、问答或校对时调用其配置的兼容 API；
- 用户主动使用 Git、GitHub 或其他同步工具上传文件。

本地优先不等于完全离线。请只处理你有权访问和保存的内容，并在提交前检查暂存区。
